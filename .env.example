# Balldontlie API Configuration
BALLDONTLIE_API_KEY=your_api_key_here

# Database Configuration
# For local development (SQLite):
DATABASE_URL=sqlite:///./nba_analytics.db

# For production (Railway will provide this automatically):
# DATABASE_URL=postgresql://user:password@host:port/dbname

# Railway will automatically set this variable, so you typically don't need to set it manually
